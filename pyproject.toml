[project]
name = "payment-collection-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pydantic>=2.0,<3.0",
]

[project.scripts]
payment-collection-agent = "main:main"

[tool.setuptools]
py-modules = [
    "agent",
    "api_client",
    "evaluation",
    "extractor",
    "main",
    "messages",
    "models",
    "parsers",
    "redaction",
]
